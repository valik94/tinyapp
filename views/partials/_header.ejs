<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav w-25">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <div class="w-75 text-right"> 
        <% if (username) {%>
          <form action='/logout' method="POST" >
            <span> Logged in as: Tiny App <%= username %>!</span> 
            <button type="submit" class="btn border border-dark">Log out</button>
          </form>
         <% } else { %>
          <form action='/login' method="POST" >
            <input type="username" name="username" class="form-control-inline"> 
            <button type="submit" class="btn border border-dark">Login</button>
          </form>
         <% } %>
        </div>
    </div>
  </nav>
</header>

